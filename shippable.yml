# Language setting
language: node_js

# Version number
node_js:
  - 0.12

branches:
  only:
    - master
    
build:
  pre_ci:
    - echo "this is master branch"

  ci:
    - echo "this is ci"

  post_ci:
    - ls -a
    - pwd

