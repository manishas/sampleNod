# Language setting
language: node_js

# Version number
node_js:
  - 0.12

branches:
  only:
    - master
    
env:    
  - IMAGE_TAG=dev BUILD_TASK=test
    
build:
  ci:
    - echo "this is ci"

  post_ci:
    - ls -a
    - pwd

